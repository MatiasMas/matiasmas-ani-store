const NavBar = () => {
    return (
        <nav>
            <span>AniStore</span>
            <ul>
                <a href="#">Highlights</a>
                <a href="#">Login</a>
                <a href="#">Register</a>
            </ul>
        </nav>
    );
}

export default NavBar;